import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder – app compiles', () {
    // Placeholder test so that `flutter test` has something to run.
    expect(1 + 1, 2);
  });
}
